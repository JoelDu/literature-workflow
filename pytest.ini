[pytest]
# 只收集 tests/ 目录。
# 仓库根目录的 test_api.py 是手动跑的 MinerU 联调脚本，不是单测：它在 import 期就
# 校验 TEST_PDF_PATH 并 sys.exit(1)，被 pytest 收集到会直接 INTERNALERROR、
# 一个测试都跑不了。别人 clone 下来第一件事往往就是敲 pytest，别让他撞这个。
testpaths = tests
